generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_access {
  id_access  BigInt   @id(map: "r_access_pk") @default(autoincrement())
  ins_at     DateTime @default(now()) @db.Timestamp(6)
  upd_at     DateTime @default(now()) @db.Timestamp(6)
  is_active  Boolean  @default(true)
  id_module  BigInt
  id_role    BigInt
  is_insert  Boolean  @default(false)
  is_update  Boolean  @default(false)
  is_delete  Boolean  @default(false)
  additional Json?
  r_module   r_module @relation(fields: [id_module], references: [id_module], map: "r_module_r_access_fk")
  r_role     r_role   @relation(fields: [id_role], references: [id_role], map: "r_role_r_access_fk")

  @@unique([id_module, id_role], map: "r_access_unique")
  @@index([id_module], map: "idx_r_access_id_module")
  @@index([id_role], map: "idx_r_access_id_role")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_asset {
  id_asset         BigInt         @id(map: "r_asset_pk") @default(autoincrement())
  ins_at           DateTime       @default(now()) @db.Timestamp(6)
  upd_at           DateTime       @default(now()) @db.Timestamp(6)
  is_active        Boolean        @default(true)
  uin              String         @db.VarChar
  tp_asset         String         @db.VarChar
  status           String         @db.VarChar
  book_value       Decimal        @default(0) @db.Decimal(18, 2)
  appraised_value  Decimal        @default(0) @db.Decimal(18, 2)
  expected_value   Decimal        @default(0) @db.Decimal(18, 2)
  reserve_price    Decimal        @default(0) @db.Decimal(18, 2)
  starting_bid     Decimal        @default(0) @db.Decimal(18, 2)
  realized_value   Decimal        @default(0) @db.Decimal(18, 2)
  location_city    String         @db.VarChar
  location_address String         @db.VarChar
  version_number   Int            @default(1)
  additional       Json?
  r_attach         r_attach[]
  r_auction        r_auction[]
  r_connection     r_connection[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model r_attach {
  id_attach       BigInt   @id(map: "r_attach_pk") @default(autoincrement())
  ins_at          DateTime @default(now()) @db.Timestamp(6)
  upd_at          DateTime @default(now()) @db.Timestamp(6)
  is_active       Boolean  @default(true)
  id_asset        BigInt
  tp_attach       String   @db.VarChar
  file_name       String?  @db.VarChar
  mime_type       String?  @db.VarChar
  file_size_bytes BigInt?
  file_hash       String?  @db.VarChar(64)
  file_content    Bytes?
  additional      Json?
  r_asset         r_asset  @relation(fields: [id_asset], references: [id_asset], map: "r_asset_r_attach_fk")

  @@unique([id_asset, file_hash], map: "r_attach_unique")
  @@index([id_asset], map: "idx_r_attach_id_asset")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_auction {
  id_auction BigInt   @id(map: "r_auction_pk") @default(autoincrement())
  ins_at     DateTime @default(now()) @db.Timestamp(6)
  upd_at     DateTime @default(now()) @db.Timestamp(6)
  is_active  Boolean  @default(true)
  tp_auction String   @db.VarChar
  id_event   BigInt
  id_asset   BigInt
  additional Json?
  r_asset    r_asset  @relation(fields: [id_asset], references: [id_asset], map: "r_asset_r_auction_x_asset_fk")
  r_event    r_event  @relation(fields: [id_event], references: [id_event], map: "r_auction_r_auction_x_asset_fk")
  r_bid      r_bid[]

  @@unique([id_event, id_asset], map: "r_auction_unique")
  @@index([id_asset], map: "idx_r_auction_id_asset")
  @@index([id_event], map: "idx_r_auction_id_event")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_bid {
  id_bid     BigInt    @id(map: "r_bid_pk") @default(autoincrement())
  ins_at     DateTime  @default(now()) @db.Timestamp(6)
  upd_at     DateTime  @default(now()) @db.Timestamp(6)
  is_active  Boolean   @default(true)
  id_auction BigInt
  id_user    BigInt
  value      Decimal   @default(0) @db.Decimal(18, 2)
  additional Json?
  r_auction  r_auction @relation(fields: [id_auction], references: [id_auction], map: "r_auction_x_asset_r_bid_fk")
  r_user     r_user    @relation(fields: [id_user], references: [id_user], onDelete: NoAction, onUpdate: NoAction, map: "r_user_r_bid_fk")

  @@unique([id_auction, value, id_user], map: "r_bid_unique")
  @@index([id_auction], map: "idx_r_bid_id_auction")
  @@index([id_user], map: "idx_r_bid_id_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_company {
  id_company   BigInt         @id(map: "r_company_pk") @default(autoincrement())
  ins_at       DateTime       @default(now()) @db.Timestamp(6)
  upd_at       DateTime       @default(now()) @db.Timestamp(6)
  is_active    Boolean        @default(true)
  uin          String         @db.VarChar
  company      String         @db.VarChar
  additional   Json?
  r_connection r_connection[]
  r_invitation r_invitation[]
  r_user       r_user[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_connection {
  id_connection BigInt    @id(map: "r_connection_pk") @default(autoincrement())
  ins_at        DateTime  @default(now()) @db.Timestamp(6)
  upd_at        DateTime  @default(now()) @db.Timestamp(6)
  is_active     Boolean   @default(true)
  id_company    BigInt
  id_asset      BigInt
  tp_connection String    @db.VarChar
  additional    Json?
  r_asset       r_asset   @relation(fields: [id_asset], references: [id_asset], map: "r_asset_r_connection_fk")
  r_company     r_company @relation(fields: [id_company], references: [id_company], map: "r_company_r_connection_fk")

  @@index([id_asset], map: "idx_r_connection_asset")
  @@index([id_company], map: "idx_r_connection_company")
  @@index([id_asset], map: "idx_r_connection_id_asset")
  @@index([id_company], map: "idx_r_connection_id_company")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_event {
  id_event     BigInt         @id(map: "r_event_pk") @default(autoincrement())
  ins_at       DateTime       @default(now()) @db.Timestamp(6)
  upd_at       DateTime       @default(now()) @db.Timestamp(6)
  is_active    Boolean        @default(true)
  tp_event     String         @db.VarChar
  start_at     DateTime       @default(now()) @db.Timestamp(6)
  end_at       DateTime       @default(now()) @db.Timestamp(6)
  additional   Json?
  r_auction    r_auction[]
  r_invitation r_invitation[]
}

model r_invitation {
  id_invitation BigInt    @id(map: "r_invitation_pk") @default(autoincrement())
  ins_at        DateTime  @default(now()) @db.Timestamp(6)
  upd_at        DateTime  @default(now()) @db.Timestamp(6)
  is_active     Boolean   @default(true)
  id_event      BigInt
  id_company    BigInt
  additional    Json?
  r_event       r_event   @relation(fields: [id_event], references: [id_event], map: "r_event_r_invitation_fk")
  r_company     r_company @relation(fields: [id_company], references: [id_company], map: "r_company_r_invitation_fk")

  @@unique([id_event, id_company], map: "r_invitation_unique")
  @@index([id_event], map: "idx_r_invitation_id_event")
  @@index([id_company], map: "idx_r_invitation_id_company")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_log {
  id_log BigInt   @id(map: "r_log_pk") @default(autoincrement())
  ins_at DateTime @default(now()) @db.Timestamp(6)
  tp_log String?  @db.VarChar
  log    Json

  @@index([ins_at(sort: Desc)])
  @@index([tp_log])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_module {
  id_module  BigInt     @id(map: "r_module_pk") @default(autoincrement())
  ins_at     DateTime   @default(now()) @db.Timestamp(6)
  upd_at     DateTime   @default(now()) @db.Timestamp(6)
  is_active  Boolean    @default(true)
  module     String     @db.VarChar
  is_admin   Boolean    @default(false)
  additional Json?
  r_access   r_access[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_role {
  id_role    BigInt     @id(map: "r_role_pk") @default(autoincrement())
  ins_at     DateTime   @default(now()) @db.Timestamp(6)
  upd_at     DateTime   @default(now()) @db.Timestamp(6)
  is_active  Boolean    @default(true)
  role       String     @db.VarChar
  additional Json?
  r_access   r_access[]
  r_user     r_user[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model r_user {
  id_user        BigInt         @id(map: "r_user_pk") @default(autoincrement())
  ins_at         DateTime       @default(now()) @db.Timestamp(6)
  upd_at         DateTime       @default(now()) @db.Timestamp(6)
  is_active      Boolean        @default(true)
  id_company     BigInt
  id_role        BigInt
  uin            String         @db.VarChar
  name           String         @db.VarChar
  user_1         String         @map("user") @db.VarChar
  authentication Json
  additional     Json?
  r_bid          r_bid[]
  r_company      r_company      @relation(fields: [id_company], references: [id_company], map: "r_company_r_user_fk")
  r_role         r_role         @relation(fields: [id_role], references: [id_role], map: "r_role_r_user_fk")
  r_password_reset_token r_password_reset_token[]

  @@index([id_company], map: "idx_r_user_id_company")
  @@index([id_role], map: "idx_r_user_id_role")
}

/// Password reset tokens for secure password recovery
model r_password_reset_token {
  id_token     BigInt   @id(map: "r_password_reset_token_pk") @default(autoincrement())
  ins_at       DateTime @default(now()) @db.Timestamp(6)
  upd_at       DateTime @default(now()) @db.Timestamp(6)
  is_active    Boolean  @default(true)
  id_user      BigInt
  token_hash   String   @unique(map: "r_password_reset_token_token_hash_key") @db.VarChar
  expires_at   DateTime @db.Timestamp(6)
  used_at      DateTime? @db.Timestamp(6)
  ip_address   String?  @db.VarChar
  user_agent   String?  @db.Text
  additional   Json?
  r_user       r_user   @relation(fields: [id_user], references: [id_user], map: "r_user_r_password_reset_token_fk")

  @@index([id_user], map: "idx_r_password_reset_token_id_user")
  @@index([token_hash], map: "idx_r_password_reset_token_token_hash")
  @@index([expires_at], map: "idx_r_password_reset_token_expires_at")
}
