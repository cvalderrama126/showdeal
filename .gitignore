# Logs
*.log
logs/

# Node / build artifacts (defensive — App/ has its own .gitignore)
node_modules/
# App/node_modules is intentionally vendored & committed for offline installs.
!/App/node_modules/
!/App/node_modules/**
dist/
build/

# Environment / secrets
.env
.env.local
.env.*.local

# Paquete on-premise y exportaciones Docker: se generan para la entrega, no para Git.
deploy/
showdeal-api-local.tar
postgres-15-alpine.tar
redis-7-alpine.tar

# Editor / OS
.DS_Store
.idea/
.vscode/
